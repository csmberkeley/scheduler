<%= form_tag(create_response_path, :remote => true) do |f| %>
	<div class = "field">
		<h4>Add Comment</h4>
		<div class = "row container">
			<%= text_area_tag :body, nil, :class => "col-sm-5 col-xs-12", :style => "height:100px" %>
		</div>
		<div class = "row container">
			Want to switch? <%= check_box_tag "switch" %>
		</div>
		<%= hidden_field_tag :offer_id, :value => offer.id %>
	</div>
	<div class = "actions">
		<%= submit_tag "Add" %>
	</div>
<% end %>
